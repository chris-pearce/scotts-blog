// @flow
import { ROOT_FONT_SIZE } from './root-font-size';

const BREAKPOINTS = Object.freeze({
  small2x: null,
  smallx: null,
  small: null,
  palm: `${600 / ROOT_FONT_SIZE}em`,
  lap: null,
  desk: null,
});

export { BREAKPOINTS };
