// @flow
import { toEm } from 'utilities';

const BREAKPOINTS = Object.freeze({
  small2x: null,
  smallx: null,
  small: null,
  medium: toEm(600),
  large: toEm(780),
  largex: null,
  large2x: null,
});

export { BREAKPOINTS };
